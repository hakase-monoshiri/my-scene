<%= form_for artist do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %><br>

    <%= f.label :hometown %>
    <%= f.text_area :hometown %><br>

    <%= f.label :bio %>
    <%= f.text_area :bio %><br>

  <%= f.submit %>
<% end %> 