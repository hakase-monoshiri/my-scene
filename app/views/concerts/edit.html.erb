<%= form_tag concert_path(@concert) do %>
 <%= render partial: 'form', locals: {concert: @concert} %>
<% end %> 

<br>

<%= back_to_artist_or_venue_concerts(@concert) %>

<%= button_to "Delete Concert", concert_path(@concert), method: :delete %> 
